@model UserModel

<h2>Delete User</h2>

@using (Html.BeginForm("Delete", "YourControllerName", FormMethod.Post))
{
    @Html.HiddenFor(m => m.Id)

    <p>Are you sure you want to delete this user?</p>
    <input type="submit" value="Delete" />
    @Html.ActionLink("Cancel", "Index")
}